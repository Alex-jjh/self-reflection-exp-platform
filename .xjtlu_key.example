# XJTLU AI gateway key for cross-model comparison (tools/model_compare.py).
# Endpoint: https://aiagent.xjtlu.edu.cn/api/aigw/v1
# Not used by app.py — the study instrument runs on Bedrock only.
# Copy to .xjtlu_key (gitignored) and paste the app_api_key on one line.
PASTE-KEY-HERE
